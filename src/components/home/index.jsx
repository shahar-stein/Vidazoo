import React from "react";

export default function Home() {
  return <div>Hey</div>;
}


/* הצאט אמר להוסיף את זה 
import axios from 'axios';
const domain = 'example.com';
axios.get('/get-ads', {
  params: {
    domain
  }
})
  .then(response => {
    console.log(response.data); // Logs the response data from the server
  })
  .catch(error => {
    console.error(error);
  });
*/
